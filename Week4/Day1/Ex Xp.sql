-- CREATE TABLE items(
--    id serial primary key,
--    utility text,
--    price integer
-- );

-- INSERT INTO items (id, utility, price)
-- VALUES
-- (1, 'Small Desk', 100),
-- (2, 'Large desk', 300),
-- (3, 'Fan', 80);

-- SELECT * FROM items;
-- SELECT * FROM items where price > 80;
-- SELECT * FROM items where price < 300;



-- CREATE TABLE customers(
--    id serial primary key,
--    fname text,
--    lname text
-- );

-- INSERT INTO customers (id, fname, lname)
-- VALUES
-- (1, 'GregSandraScott', 'Jones'),
-- (2, 'SandraScott', 'Jones'),
-- (3, 'Scott', 'Scott'),
-- (4, 'Trevor', 'MelanieGreen'),
-- (5, 'Melanie', 'Johnson');

-- SELECT * FROM customers;
-- SELECT * FROM customers WHERE lname = 'Smith';
-- SELECT * FROM customers WHERE lname = 'Jones';
-- SELECT * FROM customers WHERE lname != 'Jones';
